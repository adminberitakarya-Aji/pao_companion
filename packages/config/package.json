{
  "name": "@pao/config",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "echo \"no build needed\" && exit 0",
    "lint": "echo \"no lint needed\" && exit 0",
    "typecheck": "echo \"no typecheck needed\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
