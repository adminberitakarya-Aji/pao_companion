// "user" = pesan dari pengguna, "companion" = balasan AI companion.
// Nama disengaja netral (bukan "assistant"/"bot") supaya jelas ini
// konteks companion chat, bukan asisten generik.
export type MessageRole = "user" | "companion";
