{
  "name": "@pao/database",
  "version": "0.1.0",
  "private": true,
  "main": "prisma/client.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "dotenv -e ../../../.env -- prisma migrate dev",
    "studio": "dotenv -e ../../../.env -- prisma studio",
    "seed": "dotenv -e ../../../.env -- ts-node seeds/seed.ts",
    "build": "echo \"database ready\" && exit 0",
    "lint": "echo \"no lint for prisma package\" && exit 0",
    "typecheck": "echo \"no typecheck for prisma package\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^5.16.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.16.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
